{
  "name": "skyshield-demo-app",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal demo service used as a build/scan target for SkyShield's container security pipeline (Phase 4).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
